/**
 * Handler Validation
 *
 * This module is no longer used - all handlers are now generated.
 * Kept as a placeholder in case validation is needed in the future.
 */

// Validation is handled by Ponder's built-in codegen
// No custom validation logic required
console.log(
  '[validation] Using generated handlers - no custom validation needed',
);
